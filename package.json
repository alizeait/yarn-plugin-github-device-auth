{
  "name": "yarn-plugin-github-device-auth",
  "description": "Automatically authenticate GitHub packages in Yarn with GitHub Device Flow ",
  "version": "0.0.0",
  "author": "alizeait",
  "license": "MIT",
  "main": "./src/index.ts",
  "scripts": {
    "build": "builder build plugin",
    "build:dev": "builder build plugin --no-minify",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "oxlint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check-types": "tsc --noEmit"
  },
  "keywords": [],
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@yarnpkg/builder": "^4.2.3",
    "oxlint": "^1.24.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.1"
  },
  "dependencies": {
    "@clack/prompts": "^1.0.0-alpha.7",
    "@types/copy-paste": "^2.1.0",
    "@yarnpkg/core": "^4.5.0",
    "@yarnpkg/fslib": "^3.1.4",
    "copy-paste": "^2.2.0",
    "open": "^8.4.2",
    "picocolors": "^1.1.1"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "yarn@4.10.3"
}
